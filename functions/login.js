function login(mail, pass, radio) {
    let mail, pass, radio = mail, pass, radio;
    
    if (!main || !pass) return;
    if ((mail && pass) && !radio) {

    };
    if ((mail && pass) && radio) {

    };
};

export { login };